
func use() {
    out("so usefull rn ngl");
}

func ham(x, y, z) {
    z();
    out(x +  " is cooler than " + y + " which are cooler than flamingos (not really)");
}


udv x = in("Enter item 1: ");
udv y = in("Enter item 2: ");
ham(x,y,use); 
